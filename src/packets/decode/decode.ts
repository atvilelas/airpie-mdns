// import { Decoder, MDNSResources, Packet } from './types';

// export const decode: Decoder<Packet<MDNSResources>> = (buffer, options) => {
//   const { offset, payloadFields } = options || {};
//   const baseOffset = offset || 0;
//   let decodingOffset = baseOffset;

// };
